```json
{
  "tool": "readFile",
  "arguments": {
    "path": "src/auth/login.ts"
  }
}
```

The tool call is wrapped in a markdown code block with extra text surrounding it. This is a common quirk of Gemini or local models.
